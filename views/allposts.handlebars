<div class="posts">

  <!-- Loop over each object in the projects array -->
  {{!-- {{#each posts as |post|}} --}}
  {{#each posts as |post|}}

    <div class="card">
      <div class="card-header flex-row justify-space-between">
        <h2>
          <a href="/post/{{post.id}}">{{post.title}}</a>
        </h2>
        <div>
          By
          {{user.username}}
          on
          {{format_date createdAt}}
        </div>
      </div>

      <div class="card-body">
        {{body}}
      </div>
    </div>

    {{!-- <div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/project/{{posts.id}}">{{posts.name}}</a>
    </h2>
    <p>{{format_date post.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div> --}}

  {{/each}}
</div>